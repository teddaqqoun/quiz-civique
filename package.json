{
  "name": "quiz-civique",
  "version": "1.0.0",
  "main": "_worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "migrate": "wrangler d1 migrations apply quiz-civique-feedback --remote",
    "deploy": "npm run migrate && wrangler deploy",
    "test": "node scripts/validate-content.mjs && node scripts/test-quiz-engine.mjs && node scripts/test-feedback.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "wrangler": "^4.47.0"
  }
}
